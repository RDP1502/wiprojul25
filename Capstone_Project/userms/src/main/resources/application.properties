spring.application.name=userms

server.port=9010
spring.datasource.url = jdbc:mysql://localhost:3306/userdb2?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username = root
spring.datasource.password = cremune@2025

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

#springdoc.packagesToScan=com.example.yourpackage # Optional: Restrict scanning to specific packages
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html